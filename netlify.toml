[build]
  publish = "publish"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Routing for /api/search and /api/result is handled by each function's in-code
# `config.path`, so no redirects are needed here.
